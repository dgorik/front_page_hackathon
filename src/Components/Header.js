import React from 'react';
import './LandingPage.css'; // Ensure to import the appropriate CSS

const Header = () => {
  return (
    <header className="header">
      <div className="landing-container">
        <h1 className="header-title">Misfits AI</h1>
        <p className="header-subtitle">Innovating the Future of Construction with AI</p>
      </div>
    </header>
  );
};

export default Header;